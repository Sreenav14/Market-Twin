# MarketTwin Shared Python

Shared Python code that is safe to reuse across backend components.

This package may contain:

- Shared identifiers
- Common event envelope models
- Logging helpers
- Error models
- Tracing helpers
- Shared configuration primitives

Business logic that belongs to one service must not be placed here.
